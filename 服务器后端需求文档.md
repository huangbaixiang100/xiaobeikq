# 小北健康 · 服务器后端需求文档

> 文档版本：v1.1  
> 更新日期：2026-07-29  
> 目标服务器：`60.28.106.46`  
> 用途：在现有 ML API 服务上扩展账号、病例、医生复核、报告、训练池，数据全部存本机  
> **数据库方案：无 sudo 权限，采用 SQLite（默认）**

---

## 一、总体说明

### 1.1 要做什么

在现有 **FastAPI + PyTorch 分析服务**（端口 `15025`）上，**新增业务模块**，实现：

| 模块 | 功能 |
|------|------|
| 账号 | 用户注册/登录；医生固定账号（见第七章） |
| 病例 | 上传图片、调用现有 analyze、入库 |
| 复核 | 医生待办队列、确认/校正 |
| 报告 | 专业版 + 患者简要版 |
| 训练池 | 复核后样本回流 |

**前端网页仍部署在 Vercel**，通过 HTTP 直接调用 `http://60.28.106.46:15025` 的新接口（或经 Vercel 代理）。

### 1.2 不做什么

- 不建设管理员后台 Web 页面
- 不发手机/邮箱验证码
- 首期不做微信登录
- 首期不做除「乳牙滞留」外的多病种（标签词表预留即可）

### 1.3 现有环境（已知）

| 项目 | 值 |
|------|-----|
| 服务器 | `60.28.106.46` |
| API 端口 | `15025` |
| 框架 | FastAPI + uvicorn |
| 运行环境 | conda `AgenticRAG` |
| 项目目录 | `/home/xiaobei/hbx/乳牙滞留识别/` |
| 启动脚本 | `start_api.sh` |
| 主程序 | `api_service.py`（以服务器实际文件名为准） |
| 图片目录 | `/data/xiaobei/hbx/picture` |
| ML 接口 | `POST /api/v1/analyze` 等（已有，保持不变） |
| 服务器权限 | **无 sudo** |

---

## 二、架构设计

### 2.1 推荐架构

```
Vercel 网页（用户端 + 医生端）
        │
        │  HTTPS / HTTP
        ▼
60.28.106.46:15025  FastAPI 单进程/单服务（workers=1）
        │
        ├── 已有：/api/v1/analyze、detect-mouth、classify …（ML 推理）
        │
        └── 新增：/api/v1/auth/*、cases/*、review/*、reports/*
                    │
                    ├── SQLite（项目 data/ 目录，存结构化数据）
                    └── /data/xiaobei/hbx/（本机磁盘，存图片）
```

**原则**：一个 FastAPI 应用、一个端口，ML 与业务共用；业务代码单独模块，不污染推理逻辑。

> **注意**：SQLite 与多 worker 不兼容，uvicorn 必须设 `workers=1`。

### 2.2 建议目录结构（服务器上新增）

```
/home/xiaobei/hbx/乳牙滞留识别/
├── api_service.py              # 现有入口，挂载新 router
├── routers/                    # 【新增】
│   ├── auth.py                 # 注册、登录
│   ├── cases.py                # 上传、查询病例
│   ├── review.py               # 医生复核
│   └── reports.py              # 报告生成与查询
├── models/                     # 【新增】Pydantic 请求/响应模型
│   └── schemas.py
├── db/                         # 【新增】
│   ├── database.py             # 数据库连接
│   ├── models.py               # SQLAlchemy ORM 模型
│   └── init.sql                # SQLite 建表 SQL
├── data/                       # 【新增】
│   └── xiaobei_health.db       # SQLite 数据库文件（自动创建）
├── services/                   # 【新增】业务逻辑
│   ├── auth_service.py
│   ├── case_service.py
│   ├── review_service.py
│   └── report_service.py
├── scripts/                    # 【新增】
│   └── init_db.py              # 建表 + 写入固定医生账号
└── config.py                   # 【新增】统一配置（DB、JWT、路径）
```

### 2.3 数据存储规划

| 数据类型 | 存储位置 | 说明 |
|----------|----------|------|
| 用户、病例、复核、报告 | SQLite 文件 | `/home/xiaobei/hbx/乳牙滞留识别/data/xiaobei_health.db` |
| 原图、热力图 | 本地磁盘 | 见下方路径规范 |
| JWT Secret | 环境变量 | 不写进代码 |

**图片路径规范（新增）**：

```
/data/xiaobei/hbx/
├── picture/                    # 现有 ML 保存目录（可保留兼容）
└── cases/                      # 【新增】按病例组织
    └── {case_id}/
        ├── original.jpg        # 原图
        └── heatmap.jpg         # 热力图（可选）
```

---

## 三、数据库需求（无 sudo：SQLite 方案）

### 3.1 数据库选型

**采用：SQLite 3**（无需安装、无需 sudo）

| 对比项 | SQLite（本方案） | PostgreSQL |
|--------|------------------|------------|
| 安装 | 不需要，Python 内置 | 需要 sudo 或管理员 |
| 数据文件 | 单个 `.db` 文件 | 独立服务进程 |
| 备份 | 复制 `.db` 文件即可 | pg_dump |
| 适用规模 | 中小流量、单进程 API | 高并发、多 worker |
| 本项目 | **默认采用** | 有 sudo 后可迁移 |

**UUID 说明**：SQLite 无 `gen_random_uuid()`，在 Python 层用 `uuid.uuid4()` 生成后写入。

**JSON 说明**：报告内容用 `TEXT` 存 JSON 字符串，或 SQLAlchemy `JSON` 类型（SQLite 后端自动处理）。

### 3.2 初始化（无 sudo，服务器执行）

```bash
conda activate AgenticRAG
cd /home/xiaobei/hbx/乳牙滞留识别

# 1. 创建数据和图片目录（不需要 sudo）
mkdir -p data
mkdir -p /data/xiaobei/hbx/cases

# 2. 安装 Python 依赖（见第四章，无需 psycopg2）
pip install sqlalchemy python-jose[cryptography] passlib[bcrypt] python-multipart aiofiles

# 3. 配置环境变量（写入 start_api.sh 或 .env，见 4.2）

# 4. 执行建表 + 初始化固定医生账号
python scripts/init_db.py
```

`init_db.py` 职责：

1. 若 `data/xiaobei_health.db` 不存在则创建
2. 执行 `db/init.sql` 建表
3. 写入固定医生账号（见第七章）
4. 开启 SQLite WAL 模式（提升并发读性能）

### 3.3 DATABASE_URL 配置

**绝对路径写法**（推荐，避免工作目录变化导致找不到库）：

```bash
DATABASE_URL=sqlite:////home/xiaobei/hbx/乳牙滞留识别/data/xiaobei_health.db
```

> 注意：SQLite 的 URL 是 4 个斜杠——`sqlite:///` + 绝对路径（`/home/...`）。

**SQLAlchemy 连接参数建议**（写在 `db/database.py`）：

```python
engine = create_engine(
    DATABASE_URL,
    connect_args={"check_same_thread": False},  # FastAPI 多线程需要
)

# 启动时执行
with engine.connect() as conn:
    conn.execute(text("PRAGMA journal_mode=WAL"))
    conn.execute(text("PRAGMA foreign_keys=ON"))
```

### 3.4 SQLite 建表 SQL

保存为 `db/init.sql`，由 `scripts/init_db.py` 执行：

```sql
-- 用户表
CREATE TABLE IF NOT EXISTS users (
    id            TEXT PRIMARY KEY,           -- UUID 字符串，Python 生成
    username      TEXT UNIQUE NOT NULL,
    password_hash TEXT NOT NULL,
    role          TEXT NOT NULL CHECK (role IN ('user', 'doctor')),
    register_ip   TEXT,
    created_at    TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE INDEX IF NOT EXISTS idx_users_register_ip_created
    ON users (register_ip, created_at);

-- 病例表
CREATE TABLE IF NOT EXISTS cases (
    id            TEXT PRIMARY KEY,
    user_id       TEXT REFERENCES users(id) ON DELETE SET NULL,
    image_path    TEXT NOT NULL,
    image_hash    TEXT,
    status        TEXT NOT NULL DEFAULT 'pending_review'
                  CHECK (status IN ('pending_review', 'confirmed', 'corrected', 'archived')),
    source        TEXT NOT NULL CHECK (source IN ('guest', 'registered')),
    created_at    TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE INDEX IF NOT EXISTS idx_cases_status_created
    ON cases (status, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_cases_user_id ON cases (user_id);

-- 机器判定结果
CREATE TABLE IF NOT EXISTS ai_results (
    id            TEXT PRIMARY KEY,
    case_id       TEXT NOT NULL UNIQUE REFERENCES cases(id) ON DELETE CASCADE,
    label         TEXT NOT NULL,
    confidence    REAL NOT NULL,
    heatmap_path  TEXT,
    raw_json      TEXT,                       -- JSON 字符串
    created_at    TEXT NOT NULL DEFAULT (datetime('now'))
);

-- 医生复核记录
CREATE TABLE IF NOT EXISTS reviews (
    id            TEXT PRIMARY KEY,
    case_id       TEXT NOT NULL UNIQUE REFERENCES cases(id) ON DELETE CASCADE,
    doctor_id     TEXT NOT NULL REFERENCES users(id),
    action        TEXT NOT NULL CHECK (action IN ('confirm', 'correct')),
    doctor_label  TEXT,
    note          TEXT,
    reviewed_at   TEXT NOT NULL DEFAULT (datetime('now'))
);

-- 报告表
CREATE TABLE IF NOT EXISTS reports (
    id            TEXT PRIMARY KEY,
    case_id       TEXT NOT NULL REFERENCES cases(id) ON DELETE CASCADE,
    type          TEXT NOT NULL CHECK (type IN ('professional', 'patient')),
    content       TEXT NOT NULL,              -- JSON 字符串
    created_at    TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE INDEX IF NOT EXISTS idx_reports_case_id ON reports (case_id);

-- 新收集的训练样本池
CREATE TABLE IF NOT EXISTS training_pool (
    id            TEXT PRIMARY KEY,
    case_id       TEXT NOT NULL REFERENCES cases(id) ON DELETE CASCADE,
    image_hash    TEXT,
    ai_label      TEXT NOT NULL,
    doctor_label  TEXT NOT NULL,
    note          TEXT,
    exported_at   TEXT,
    created_at    TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE INDEX IF NOT EXISTS idx_training_pool_exported ON training_pool (exported_at);
```

### 3.5 预设疾病标签词表（首期）

代码中常量数组，不必单独建表：

```python
DISEASE_LABELS = ["非乳牙滞留", "乳牙滞留", "龋病", "牙周炎"]
```

首期 ML 只输出「非乳牙滞留 / 乳牙滞留」；医生校正时可从完整词表中选。

### 3.6 SQLite 使用注意

| 项 | 要求 |
|----|------|
| uvicorn workers | 必须为 `1`，禁止多 worker 同时写 |
| 备份 | 定期复制 `data/xiaobei_health.db`（停服务后备份更安全） |
| 文件权限 | 确保运行 API 的用户对 `data/` 目录可读写 |
| 迁移到 PostgreSQL | 后期若有 sudo，SQLAlchemy 模型可复用，仅改 `DATABASE_URL` |

---

## 附录 A：PostgreSQL 方案（需 sudo，本期不采用）

> 仅在有 sudo 或管理员协助安装数据库时参考；**当前服务器请用第三章 SQLite 方案。**

<details>
<summary>点击展开 PostgreSQL 安装与建表（备用）</summary>

```bash
sudo yum install -y postgresql-server postgresql-contrib
sudo postgresql-setup initdb && sudo systemctl start postgresql
sudo -u postgres psql -c "CREATE USER xiaobei WITH PASSWORD '强密码';"
sudo -u postgres psql -c "CREATE DATABASE xiaobei_health OWNER xiaobei;"
```

```bash
DATABASE_URL=postgresql://xiaobei:强密码@localhost:5432/xiaobei_health
```

需额外安装：`pip install psycopg2-binary`

PostgreSQL 建表 SQL 见 git 历史 v1.0 版文档，或使用 SQLAlchemy `Base.metadata.create_all()` 自动生成。

</details>

---

## 四、环境与依赖

### 4.1 Python 新增依赖

在 conda 环境 `AgenticRAG` 中安装：

```bash
conda activate AgenticRAG
pip install sqlalchemy python-jose[cryptography] passlib[bcrypt] python-multipart aiofiles
```

| 包 | 用途 |
|----|------|
| sqlalchemy | ORM，连接 SQLite（内置驱动，无需 psycopg2） |
| python-jose | JWT 签发与校验 |
| passlib[bcrypt] | 密码哈希 |
| aiofiles | 异步写图片文件 |

### 4.2 环境变量（写入 start_api.sh 或 .env）

```bash
# 数据库（SQLite，无 sudo）
DATABASE_URL=sqlite:////home/xiaobei/hbx/乳牙滞留识别/data/xiaobei_health.db

# JWT（用 openssl rand -hex 32 生成）
JWT_SECRET=请替换为随机长字符串
JWT_EXPIRE_HOURS=168

# 路径
CASE_IMAGE_DIR=/data/xiaobei/hbx/cases

# 业务
REGISTER_LIMIT_PER_IP_PER_DAY=10
```

### 4.3 磁盘目录（无 sudo）

```bash
# 数据库目录（在项目内，一定有写权限）
mkdir -p /home/xiaobei/hbx/乳牙滞留识别/data

# 图片目录（现有 ML 已在用 picture/，同级建 cases/ 即可）
mkdir -p /data/xiaobei/hbx/cases
chmod 755 /data/xiaobei/hbx/cases
```

若 `cases/` 创建失败，可退而求其次放在项目内：

```bash
CASE_IMAGE_DIR=/home/xiaobei/hbx/乳牙滞留识别/data/cases
mkdir -p /home/xiaobei/hbx/乳牙滞留识别/data/cases
```

---

## 五、接口需求（详细）

**统一约定**：

- Base URL：`http://60.28.106.46:15025`
- 前缀：新增接口均放在 `/api/v1/` 下
- 认证：除注册、登录、访客查 case 外，请求头带 `Authorization: Bearer <token>`
- 时间：ISO 8601 字符串
- 错误格式：

```json
{
  "success": false,
  "error_code": "RATE_LIMIT_EXCEEDED",
  "message": "该 IP 今日注册次数已达上限（10 次），请明天再试"
}
```

---

### 5.1 账号模块

#### POST `/api/v1/auth/register` — 用户注册

**权限**：公开（仅创建 `role=user`）

**请求 Body（JSON）**：

```json
{
  "username": "zhangsan",
  "password": "abc12345"
}
```

**校验规则**：

- `username`：4–20 位，字母数字下划线
- `password`：≥8 位，至少含字母和数字
- 同 IP 当日注册数 `< 10`（查 `users` 表 `register_ip` + `created_at`）
- 用户名不可重复；**不可注册为 `doctor`**（医生账号固定，见第七章）

**成功响应 201**：

```json
{
  "success": true,
  "message": "注册成功",
  "data": {
    "user_id": "uuid",
    "username": "zhangsan",
    "token": "jwt..."
  }
}
```

**失败**：409 用户名已存在；429 IP 超限；400 参数不合法

---

#### POST `/api/v1/auth/login` — 登录

**请求 Body**：

```json
{
  "username": "zhangsan",
  "password": "abc12345"
}
```

**医生登录示例**：

```json
{
  "username": "doctor",
  "password": "xbkouqiang"
}
```

**成功响应 200**：

```json
{
  "success": true,
  "data": {
    "user_id": "uuid",
    "username": "doctor",
    "role": "doctor",
    "token": "jwt..."
  }
}
```

**失败**：401 用户名或密码错误

**JWT Payload 建议**：

```json
{
  "sub": "user_uuid",
  "username": "doctor",
  "role": "doctor",
  "exp": 1234567890
}
```

---

#### GET `/api/v1/auth/me` — 当前用户

**权限**：需登录

**成功响应 200**：

```json
{
  "success": true,
  "data": {
    "user_id": "uuid",
    "username": "zhangsan",
    "role": "user"
  }
}
```

---

### 5.2 病例模块

#### POST `/api/v1/cases/upload` — 上传并分析

**权限**：

- 访客：不带 token（`source=guest`，`user_id=NULL`）
- 注册用户：带 token（`source=registered`）

**请求**：`multipart/form-data`

| 字段 | 类型 | 必填 |
|------|------|------|
| file | 图片文件 | 是 |

**处理流程（必须按序）**：

1. 生成 `case_id`（UUID）
2. 保存原图到 `{CASE_IMAGE_DIR}/{case_id}/original.jpg`
3. 计算 `image_hash`（SHA256）
4. **调用现有 analyze 逻辑**（不要 HTTP 自调，直接 import 内部函数）
5. 写入 `cases` 表，`status=pending_review`
6. 写入 `ai_results` 表
7. 返回 case_id + AI 结果 + 查询链接

**成功响应 201**：

```json
{
  "success": true,
  "data": {
    "case_id": "uuid",
    "status": "pending_review",
    "source": "guest",
    "ai_result": {
      "label": "乳牙滞留",
      "confidence": 0.99,
      "probabilities": {
        "非乳牙滞留": 0.01,
        "乳牙滞留": 0.99
      },
      "recommendations": ["..."]
    },
    "report_url": "/api/v1/cases/{case_id}",
    "message": "已提交，待医生复核"
  }
}
```

---

#### GET `/api/v1/cases/{case_id}` — 查询病例详情

**权限**：公开（任何人凭 case_id 可查，便于快速使用用户）

**成功响应 200**：

```json
{
  "success": true,
  "data": {
    "case_id": "uuid",
    "status": "pending_review",
    "source": "guest",
    "created_at": "2026-07-29T10:00:00+08:00",
    "ai_result": {
      "label": "乳牙滞留",
      "confidence": 0.99
    },
    "review": null,
    "reports": {
      "patient": null,
      "professional": null
    }
  }
}
```

复核完成后 `review` 和 `reports.patient` 有值；`status` 为 `confirmed` 或 `corrected`。

---

#### GET `/api/v1/cases/my` — 我的病例列表

**权限**：登录用户（`role=user`）

**Query 参数**：

| 参数 | 说明 | 默认 |
|------|------|------|
| page | 页码 | 1 |
| page_size | 每页条数 | 20 |

**成功响应 200**：

```json
{
  "success": true,
  "data": {
    "total": 5,
    "items": [
      {
        "case_id": "uuid",
        "status": "confirmed",
        "ai_label": "乳牙滞留",
        "confidence": 0.99,
        "doctor_label": "乳牙滞留",
        "created_at": "2026-07-29T10:00:00+08:00"
      }
    ]
  }
}
```

---

### 5.3 医生复核模块

**所有接口要求 `role=doctor`**

#### GET `/api/v1/review/list` — 待复核列表

**Query 参数**：

| 参数 | 说明 | 默认 |
|------|------|------|
| status | `pending` / `all` | `pending` |
| days | 1=今日，7=近7日，0=不限 | 7 |
| page | 页码 | 1 |
| page_size | 每页 | 20 |

**成功响应 200**：

```json
{
  "success": true,
  "data": {
    "pending_count": 12,
    "total": 12,
    "items": [
      {
        "case_id": "uuid",
        "thumbnail_url": "/api/v1/cases/{case_id}/image",
        "ai_label": "乳牙滞留",
        "confidence": 0.99,
        "source": "guest",
        "created_at": "2026-07-29T10:00:00+08:00"
      }
    ]
  }
}
```

---

#### GET `/api/v1/review/{case_id}` — 复核详情

**成功响应 200**：

```json
{
  "success": true,
  "data": {
    "case_id": "uuid",
    "status": "pending_review",
    "image_url": "/api/v1/cases/{case_id}/image",
    "ai_result": {
      "label": "乳牙滞留",
      "confidence": 0.99,
      "probabilities": {},
      "recommendations": []
    },
    "source": "registered",
    "created_at": "..."
  }
}
```

---

#### POST `/api/v1/review/{case_id}/confirm` — 确认 AI 判定正确

**请求 Body**：空 或 `{}`

**处理流程**：

1. 校验 case 存在且 `status=pending_review`
2. 插入 `reviews`（`action=confirm`，`doctor_label=ai_label`）
3. 更新 `cases.status=confirmed`
4. **自动生成双版报告**（见 5.4）
5. 写入 `training_pool`（`ai_label` 与 `doctor_label` 相同）
6. 更新 `cases.status=archived`

**成功响应 200**：

```json
{
  "success": true,
  "message": "已确认",
  "data": {
    "case_id": "uuid",
    "status": "archived"
  }
}
```

---

#### POST `/api/v1/review/{case_id}/correct` — 校正判定

**请求 Body**：

```json
{
  "label": "龋病",
  "note": "图像显示明显龋坏，非乳牙滞留"
}
```

**校验**：

- `label` 必须在 `DISEASE_LABELS` 中
- `note` 必填，1–500 字

**处理流程**：同 confirm，但 `action=correct`，`doctor_label=label`；报告里体现机器 vs 医生差异。

**成功响应 200**：

```json
{
  "success": true,
  "message": "已校正并生成报告",
  "data": {
    "case_id": "uuid",
    "status": "archived",
    "doctor_label": "龋病"
  }
}
```

---

#### GET `/api/v1/cases/{case_id}/image` — 获取原图

**权限**：公开（首期 case_id 即凭证）

**响应**：返回 `image/jpeg` 文件流

---

### 5.4 报告模块

#### POST `/api/v1/reports/{case_id}/generate` — 生成报告

**说明**：一般由 confirm/correct 内部自动调用；也可单独暴露供调试。

**专业版 `professional`**（JSON 结构示例）：

```json
{
  "case_id": "uuid",
  "patient_source": "guest",
  "images": { "original": "..." },
  "ai_diagnosis": { "label": "乳牙滞留", "confidence": 0.99 },
  "doctor_diagnosis": { "label": "龋病", "action": "correct", "note": "..." },
  "comparison": "机器判定为乳牙滞留，医生校正为龋病",
  "recommendations": ["建议尽快就诊...", "..."],
  "reviewed_at": "...",
  "doctor_username": "doctor"
}
```

**患者简要版 `patient`**：

```json
{
  "case_id": "uuid",
  "summary": "经医生复核，您的检测结果为：龋病",
  "ai_preliminary": "AI 初步判定：乳牙滞留（99%）",
  "doctor_conclusion": "医生复核结论：龋病",
  "note": "图像显示明显龋坏，非乳牙滞留",
  "advice": ["建议尽快到口腔科就诊"],
  "status_label": "已复核"
}
```

写入 `reports` 表两条记录（`type=professional` 和 `type=patient`）。

---

#### GET `/api/v1/reports/{case_id}` — 获取报告

**Query**：`type=patient` 或 `type=professional`

**权限**：

- `patient`：公开（凭 case_id）
- `professional`：仅 doctor

---

## 六、与现有 analyze 的集成方式

**不要**在 upload 流程里 `requests.post` 调自己的 `/api/v1/analyze`（多一次网络、占 GPU 队列）。

**应该**：把 `api_service.py` 里 analyze 的核心逻辑抽成函数，例如：

```python
# services/ml_service.py
async def run_analyze(image_bytes: bytes) -> AnalyzeResult:
    ...  # 原 analyze 路由内的逻辑
```

然后：

- 原 `POST /api/v1/analyze` 路由：继续对外提供，内部调 `run_analyze`
- 新 `POST /api/v1/cases/upload`：存图 → `run_analyze` → 写库

**保持现有 analyze 接口行为不变**，避免 Vercel 代理和外部调用方受影响。

---

## 七、医生固定账号（无后台）

### 7.1 固定凭据

| 字段 | 值 |
|------|-----|
| 账号名 | `doctor` |
| 密码 | `xbkouqiang` |
| 角色 | `doctor` |

- **不开放医生自助注册**；注册接口禁止创建 `role=doctor`
- 医生账号在 **数据库初始化时写入**，不提供 Web 管理页
- 密码以 **bcrypt 哈希** 存入 `users.password_hash`，**禁止存明文**

### 7.2 初始化脚本 `scripts/init_db.py`

```bash
conda activate AgenticRAG
cd /home/xiaobei/hbx/乳牙滞留识别
python scripts/init_db.py
```

**脚本逻辑**：

1. 创建 `data/xiaobei_health.db`（若不存在）
2. 执行 `db/init.sql` 建表
3. 开启 WAL + foreign_keys
4. 检查 `users` 表是否已有 `username='doctor'`
   - 若无：插入医生账号（密码 `xbkouqiang` 经 bcrypt 哈希）
   - 若有：跳过，不覆盖（避免重复初始化改坏密码）

**脚本内密码哈希示例**：

```python
from passlib.context import CryptContext
pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
password_hash = pwd_context.hash("xbkouqiang")
```

### 7.3 验证医生登录

```bash
curl -X POST http://localhost:15025/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"doctor","password":"xbkouqiang"}'
```

期望返回 `role: "doctor"` 及 JWT token。

### 7.4 重置医生密码（可选）

若需改密码，在服务器上执行一次性 Python 命令更新哈希，或重新运行带 `--reset-doctor` 参数的 init 脚本（自行实现）：

```python
# 在服务器 Python 交互环境
from passlib.context import CryptContext
print(CryptContext(schemes=["bcrypt"]).hash("新密码"))
# 将输出更新到 users 表 WHERE username='doctor'
```

---

## 八、安全需求

| 项 | 要求 |
|----|------|
| 密码 | bcrypt 哈希，不可逆；医生密码同样哈希存储 |
| JWT | HS256，`JWT_SECRET` 足够长，不写进 Git |
| 注册限流 | 同 IP 每天 ≤10（`REGISTER_LIMIT_PER_IP_PER_DAY`） |
| 医生接口 | 校验 JWT 且 `role=doctor` |
| 医生账号 | 固定账号，init 时写入；禁止通过 register 接口创建 |
| CORS | 生产建议限制为 `https://xiaobeikq.vercel.app`、`https://xiaobeikouqiang.top` |
| 文件上传 | 仅允许 jpg/png，≤10MB，校验 MIME |
| SQL | 使用 ORM 参数化，禁止拼接 SQL |
| SQLite 文件 | 放在项目 `data/` 内，确保不对公网 HTTP 直接暴露 `.db` 文件 |
| uvicorn | `workers=1`，避免 SQLite 写锁冲突 |

---

## 九、获取客户端 IP

在 FastAPI 中：

```python
def get_client_ip(request: Request) -> str:
    forwarded = request.headers.get("x-forwarded-for")
    if forwarded:
        return forwarded.split(",")[0].strip()
    return request.client.host or "unknown"
```

注册时写入 `users.register_ip`。

**SQLite IP 限流查询示例**：

```sql
SELECT COUNT(*) FROM users
WHERE register_ip = ?
  AND role = 'user'
  AND date(created_at) = date('now', 'localtime');
```

---

## 十、实施步骤（无 sudo，服务器按序执行）

### 阶段 0：准备（约 0.5 天）

- [ ] SSH 登录 `60.28.106.46`
- [ ] `mkdir -p data` 及 `cases` 图片目录
- [ ] `pip install` 新依赖（无需 psycopg2）
- [ ] 配置 `DATABASE_URL` 等环境变量
- [ ] 运行 `python scripts/init_db.py`（建表 + 医生账号）
- [ ] 确认 `data/xiaobei_health.db` 已生成

### 阶段 1：账号（约 1 天）

- [ ] 实现 `db/database.py`、`db/models.py`
- [ ] 实现 `routers/auth.py`（register、login、me）
- [ ] 实现 IP 限流（SQLite 查询）
- [ ] 用 curl 测通用户注册/登录
- [ ] 用 curl 测通医生登录（`doctor` / `xbkouqiang`）

```bash
# 测试用户注册
curl -X POST http://localhost:15025/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test01","password":"abc12345"}'

# 测试医生登录
curl -X POST http://localhost:15025/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"doctor","password":"xbkouqiang"}'
```

### 阶段 2：上传与入库（约 1–2 天）

- [ ] 抽取 `run_analyze` 函数
- [ ] 实现 `POST /api/v1/cases/upload`
- [ ] 实现 `GET /api/v1/cases/{case_id}`
- [ ] 实现 `GET /api/v1/cases/{case_id}/image`
- [ ] 用真实图片测：SQLite 里有 case + ai_results

### 阶段 3：医生复核（约 1–2 天）

- [ ] 实现 review/list、review/{id}、confirm、correct
- [ ] 医生 JWT 权限中间件
- [ ] 用医生 token 测通确认/校正

### 阶段 4：报告与训练池（约 1 天）

- [ ] 报告模板生成（professional + patient）
- [ ] confirm/correct 后自动写 reports、training_pool
- [ ] 实现 `GET /api/v1/reports/{case_id}`
- [ ] 实现 `GET /api/v1/cases/my`

### 阶段 5：联调与上线

- [ ] 确认 uvicorn `workers=1`
- [ ] 重启 `start_api.sh`，确认 `/docs` 能看到新接口
- [ ] 从外网 curl 测 `60.28.106.46:15025`
- [ ] 前端 Vercel 改调新接口（或加代理）
- [ ] 用乳牙滞留样例图跑通全链路

---

## 十一、验收标准

以下全部通过即 P0 后端完成：

1. 用户可注册/登录；同 IP 第 11 次注册被拒绝（429）
2. 医生可用 `doctor` / `xbkouqiang` 登录，返回 `role=doctor`
3. 访客上传图片 → 返回 `case_id` + AI 结果 + `pending_review`
4. SQLite 中 `cases`、`ai_results` 有记录；磁盘有图片
5. 医生登录后可看到待复核列表
6. 医生「确认」后，case 变 `archived`，`reports` 有两条，`training_pool` 有一条
7. 医生「校正」后，报告含机器 vs 医生对照
8. 凭 `case_id` 可查到患者简要版报告
9. 注册用户 `GET /cases/my` 能看到自己的记录
10. 原有 `POST /api/v1/analyze` 仍正常工作

---

## 十二、前端对接说明（给网页开发用）

| 原逻辑 | 新逻辑 |
|--------|--------|
| 直接 POST `/api/analyze` | POST `http://60.28.106.46:15025/api/v1/cases/upload` |
| 结果仅前端展示 | 保存 `case_id`，轮询或刷新查 `/cases/{id}` |
| 无登录 | 增加注册/登录，token 存 localStorage |
| 无医生端 | 医生页调 `/review/*`，登录用 `doctor` / `xbkouqiang` |

**跨域**：服务器 CORS 需允许 Vercel 域名；或 Vercel 加代理路由（与现有 `api/analyze.js` 同理）。

---

## 十三、后续扩展（本期不做）

- 修改密码接口
- 训练池 CSV 导出接口
- 迁移到 PostgreSQL（有 sudo 后）
- 多病种模型路由
- 微信小程序 OpenID 绑定

---

*文档结束 — 无 sudo 环境下按「阶段 0 → 5」实施；数据库用 SQLite，医生账号 doctor / xbkouqiang*
