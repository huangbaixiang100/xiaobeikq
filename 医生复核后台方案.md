# 小北健康 · 医生复核后台完整方案

> 文档版本：v1.1  
> 更新日期：2026-07-29  
> 适用范围：网页端用户改造 + 医生复核后台 + 后端接口与数据库

---

## 一、项目背景

### 1.1 现状

| 模块 | 当前状态 |
|------|----------|
| 用户端 | 网页上传 → 模型判定 → 页面展示初步建议 |
| 账号体系 | 无，匿名使用 |
| 报告 | 仅前端临时展示，不持久化 |
| 医生复核 | 无 |
| 数据回流 | 无 |

### 1.2 目标

在现有流程上补齐「上传 → 模型判定 → **医生复核** → 报告 → 数据回流」闭环：

1. 模型判定后，必须有真人医生 review
2. 复核动作要轻：医生只需「对 / 错 + 改标签 + 说明」
3. 报告分两层：专业医生版 → 压缩为患者简要版
4. 校正样本写入新收集的训练样本池，用于模型迭代
5. 首期仅接「乳牙滞留」单病种跑通，后续横向扩展

---

## 二、用户角色与账号体系

### 2.1 三种入口

| 角色 | 入口 | 注册方式 | 权限 |
|------|------|----------|------|
| **访客（快速使用）** | 主页「快速使用」 | 无需注册 | 上传、查看 AI 初步结果、复核完成后通过链接查看报告 |
| **注册用户** | 主页「用户注册/登录」 | 用户名 + 密码 | 上传、历史记录、完整报告（含医生复核结论） |
| **医生** | 独立入口 `/doctor` | **不开放自助注册**，由开发者在数据库中手动创建 | 复核队列、改标签、生成报告 |

### 2.2 账号策略

#### 普通用户注册

- 注册字段：用户名（4–20 位字母数字）+ 密码（≥8 位，含字母与数字）
- **IP 限流：同一 IP 每天最多注册 10 个账号**（数据库计数）
- 不发手机/邮箱验证码，不做人机验证组件（首期以 IP 限流为主）

#### 医生账号（无管理员后台）

- **不建设管理员后台页面**，医生端仅提供登录，无注册入口
- 医生账号由开发者在 **数据库后台（如 Supabase Table Editor）** 或 **本地初始化脚本** 手动写入：

```
用户名 + bcrypt 密码哈希 + role = 'doctor'
```

- 初始密码线下发给医生；如需改密，开发者直接在数据库更新或后续再加「修改密码」页
- 仅 `role=doctor` 可访问复核相关接口与页面

#### 快速使用（访客）

- 无需账号，直接上传
- 上传后状态为「待医生复核」
- 系统返回 `caseId` 链接，复核完成后凭链接查看最终结果

---

## 三、端到端业务流程

```
用户上传口腔照片（快速使用 or 注册用户）
       ↓
服务端：存原图 + 调用模型 → 输出「疾病标签 + 置信度」（如：乳牙滞留 99%）
       ↓
写入数据库，状态 = pending_review（待医生复核）
       ↓
用户侧展示：AI 初步结果 + 「待医生复核」标注 + 报告查询链接（caseId）
       ↓
医生登录网页端 → 默认展示「未复核」队列（可按当日 / 近 7 日筛选）
       ↓
点开单条复核：
  ├─ 判定正确 → 点「确认」→ 状态 confirmed
  └─ 判定错误 → 重选标签 + 填校正说明 →「生成校正报告」→ 状态 corrected
       ↓
系统生成两层报告：
  ├─ 专业医生版（原图标注、机器 vs 医生对照、详细建议）
  └─ 患者简要版（压缩语言，供用户查看）
       ↓
样本写入训练池（图片 hash + 机器标签 + 医生标签 + 说明）
       ↓
用户查看结果：
  ├─ 注册用户 →「我的报告」列表
  └─ 快速使用 → 凭 caseId 链接查看
```

### 病例状态流转

```
pending_review  →  confirmed   （医生确认正确）
               →  corrected   （医生校正标签）
               →  archived     （报告已生成/可查看）
```

---

## 四、页面设计

### 4.1 用户端（改造现有主页）

**账号入口区**（上传区前）：

- Tab：`快速使用` | `用户登录/注册` | `医生入口`
- 快速使用：直接进入上传
- 用户注册/登录：用户名 + 密码
- 医生入口：跳转 `/doctor/login`（仅登录，无注册）

**上传结果页**：

- 展示 AI 判定（标签 + 置信度 + 建议）
- 标注「待医生复核」
- 提供报告链接 / caseId（可复制，复核完成后刷新可见最终结果）

**注册用户「我的报告」**（`/my/reports`）：

- 历史上传列表，每条含复核状态（待复核 / 已确认 / 已校正）
- 详情页：AI 判定 + 医生复核结论 + 校正说明 + 最终报告

### 4.2 医生端（新增独立子站）

| 页面 | URL | 功能 |
|------|-----|------|
| 医生登录 | `/doctor/login` | 用户名 + 密码，无注册入口 |
| 复核首页 | `/doctor/review` | 待办数徽标、时间筛选、未复核列表 |
| 复核详情 | `/doctor/review/:caseId` | 原图、机器判定、确认/校正操作 |
| 报告预览 | `/doctor/review/:caseId/report` | 专业版 / 患者简要版双视图 |

**复核首页列表字段**：缩略图 | 机器标签 | 置信度 | 上传时间 | 来源（快速使用 / 注册用户）

**复核详情操作区**：

- 判定正确 → 点「确认」
- 判定错误 → 疾病标签选择器（龋病 / 牙周炎 / 乳牙滞留等）+ 校正说明 →「生成校正报告」

---

## 五、数据模型

### 5.1 核心表

```sql
-- 用户表
users (
  id            UUID PRIMARY KEY,
  username      TEXT UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,
  role          TEXT NOT NULL,  -- 'user' | 'doctor'
  register_ip   TEXT,           -- 仅 user 注册时记录
  created_at    TIMESTAMPTZ DEFAULT NOW()
)

-- 病例表
cases (
  id            UUID PRIMARY KEY,
  user_id       UUID REFERENCES users(id),  -- NULL 表示快速使用
  image_url     TEXT NOT NULL,
  image_hash    TEXT,
  status        TEXT NOT NULL,  -- pending_review | confirmed | corrected | archived
  source        TEXT NOT NULL,  -- 'guest' | 'registered'
  created_at    TIMESTAMPTZ DEFAULT NOW()
)

-- 机器判定结果
ai_results (
  id            UUID PRIMARY KEY,
  case_id       UUID REFERENCES cases(id),
  label         TEXT NOT NULL,
  confidence    FLOAT,
  heatmap_url   TEXT,
  raw_json      JSONB,
  created_at    TIMESTAMPTZ DEFAULT NOW()
)

-- 医生复核记录
reviews (
  id            UUID PRIMARY KEY,
  case_id       UUID REFERENCES cases(id),
  doctor_id     UUID REFERENCES users(id),
  action        TEXT NOT NULL,  -- 'confirm' | 'correct'
  doctor_label  TEXT,           -- 校正后的标签（correct 时必填）
  note          TEXT,           -- 校正说明
  reviewed_at   TIMESTAMPTZ DEFAULT NOW()
)

-- 报告表
reports (
  id            UUID PRIMARY KEY,
  case_id       UUID REFERENCES cases(id),
  type          TEXT NOT NULL,  -- 'professional' | 'patient'
  content       JSONB,
  created_at    TIMESTAMPTZ DEFAULT NOW()
)

-- 新收集的训练样本池
training_pool (
  id            UUID PRIMARY KEY,
  case_id       UUID REFERENCES cases(id),
  image_hash    TEXT,
  ai_label      TEXT,
  doctor_label  TEXT,
  note          TEXT,
  exported_at   TIMESTAMPTZ,    -- 导出用于训练时标记
  created_at    TIMESTAMPTZ DEFAULT NOW()
)
```

### 5.2 数据库选型

推荐 **Supabase**（PostgreSQL + Storage，免费档可用），图片存 Storage，结构化数据存 PostgreSQL。

### 5.3 医生账号初始化（无后台方案）

**方式一：Supabase 控制台手动插入**

在 Table Editor 的 `users` 表新增一行：`username`、`password_hash`（bcrypt）、`role = 'doctor'`。

**方式二：本地脚本（推荐）**

```bash
node scripts/create-doctor.js --username dr_zhang --password xxxxxx
```

脚本内部 bcrypt 哈希后写入数据库，便于批量创建或重置密码，无需开发管理员页面。

---

## 六、接口设计

### 6.1 账号模块 `/api/auth/*`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/register` | 用户注册（含 IP 限流，仅 role=user） |
| POST | `/api/auth/login` | 登录，返回 JWT |
| GET | `/api/auth/me` | 当前用户信息 |

**注册请求**：

```json
POST /api/auth/register
{ "username": "zhangsan", "password": "abc12345" }
```

**IP 超限响应**：

```json
HTTP 429
{ "message": "该 IP 今日注册次数已达上限（10 次），请明天再试" }
```

### 6.2 病例模块 `/api/cases/*`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/cases/upload` | 上传图片（访客或登录用户） |
| GET | `/api/cases/:id` | 病例详情（含 AI 结果、复核状态、报告） |
| GET | `/api/cases/my` | 注册用户历史列表（需登录） |

### 6.3 医生复核模块 `/api/review/*`（需 doctor 角色）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/review/list?status=pending&days=7` | 待复核列表 |
| GET | `/api/review/:caseId` | 复核详情 |
| POST | `/api/review/:caseId/confirm` | 确认正确 |
| POST | `/api/review/:caseId/correct` | 校正（body: label, note） |

### 6.4 报告模块 `/api/reports/*`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/reports/:caseId/generate` | 生成双版报告（复核后触发） |
| GET | `/api/reports/:caseId` | 获取报告（用户看简要版，医生看双版） |

> 不单独建设 `/api/admin/*`；医生账号维护走数据库或本地脚本。

---

## 七、技术架构

```
┌─────────────┐     ┌─────────────┐
│  用户网页    │     │  医生网页    │
│  index.html │     │  /doctor/*  │
└──────┬──────┘     └──────┬──────┘
       │                   │
       └─────────┬─────────┘
                 ↓
    ┌────────────────────────┐
    │  Vercel Serverless API  │
    │  auth / cases / review  │
    │  reports                │
    └────────────┬───────────┘
                 ↓
    ┌────────────┴────────────┐
    ↓                         ↓
 Supabase                 现有 ML API
 (PostgreSQL + Storage)   60.28.106.46:15025
```

**与现有系统关系**：

- 保留 `/api/analyze` 代理
- 新上传流程：存图 → 写 case → 调 analyze → 写 ai_results → 进入复核队列
- 医生端为同域 `/doctor` 子路径

**新增依赖**：

- `@supabase/supabase-js` 或 `pg`
- `bcryptjs`、`jsonwebtoken`

---

## 八、分期实施计划

| 阶段 | 周期 | 内容 | 交付物 |
|------|------|------|--------|
| **P0** | 2 周 | 账号 + 病例持久化 + 复核列表 | 用户注册（IP 限流）；上传入库；医生可看未复核队列 |
| **P1** | 2 周 | 复核操作 + 双版报告 | 确认/校正流程；专业版与患者版报告 |
| **P2** | 1 周 | 用户报告页 + 训练池 | 「我的报告」；caseId 链接查询；校正样本入库 |
| **P3** | 后续 | 小程序 + 多病种 | 微信端对接；扩展龋病、牙周炎等 |

**首期验证**：用已有乳牙滞留照片跑通全链路。

---

## 九、关键设计决策

| 问题 | 方案 |
|------|------|
| 验证码 | 不发手机/邮箱验证码 |
| 注册防刷 | 同 IP 每天最多 10 账号（数据库计数） |
| 医生账号 | 不开放自助注册；**无管理员后台**，数据库/脚本手动创建 |
| 待复核展示 | 快速使用与注册用户上传后均标注「待医生复核」 |
| 结果查看 | 注册用户走「我的报告」；访客凭 caseId 链接 |
| 训练数据 | 新收集的训练样本池，与历史训练集分离 |
| 首期病种 | 乳牙滞留 |

---

## 十、新增功能清单（汇报用）

以下内容可直接复制到飞书文档。

---

### 【文字总结 · 可直接粘贴飞书】

**小北健康 · 医生复核后台 — 新增功能概述**

本次改造在现有「上传 → AI 判定 → 展示建议」基础上，补齐医生人工复核闭环，主要新增以下功能：

**一、账号体系改造**

1. 支持三种使用方式：快速使用（无需注册）、用户注册登录、医生专属入口。
2. 用户采用用户名 + 密码注册。
3. 同一 IP 每天最多注册 10 个账号，通过数据库计数防止恶意批量注册。
4. 医生账号不开放自助注册，由开发者在数据库中手动创建（不建设管理员后台页面）。

**二、医生复核后台（网页端）**

5. 医生独立登录入口与复核工作台，默认展示「未复核」队列，支持按当日/近 7 日筛选。
6. 复核详情页：左侧查看原图与机器判定（标签 + 置信度），右侧操作「确认正确」或「判定错误」。
7. 错判校正：医生从预设疾病词表（龋病/牙周炎/乳牙滞留等）重选标签，填写校正说明，一键生成校正报告。
8. 报告分层输出：先生成专业医生版报告（含机器 vs 医生对照），再自动生成患者简要版报告。

**三、用户端增强**

9. 注册用户可查看「我的报告」历史列表，报告内容包含 AI 初步判定、医生复核结论及校正说明。
10. 快速使用用户以及注册用户上传后标注「待医生复核」，复核完成后可通过链接查看最终结果。

**四、数据持久化与回流**

11. 新增数据库存储：用户、病例、AI 判定结果、复核记录、报告、新收集的训练样本池。
12. 所有图像上传后持久化保存，进入复核队列。
13. 医生确认/校正后的样本自动写入训练池（图片 hash + 机器标签 + 医生标签 + 说明），用于后续模型再训练。

**首期范围**：乳牙滞留单病种跑通全链路；后续扩展龋病、牙周炎等。

---

### 【条目清单 · 便于做表格】

| 序号 | 模块 | 新增功能 |
|------|------|----------|
| 1 | 账号 | 快速使用 / 用户注册 / 医生入口三模式 |
| 2 | 账号 | 用户名 + 密码注册 |
| 3 | 账号 | 同 IP 每日最多 10 账号（数据库限流） |
| 4 | 账号 | 医生账号数据库手动创建，不开放自助注册 |
| 5 | 医生端 | 独立登录与复核工作台 |
| 6 | 医生端 | 未复核队列 + 按当日/近 7 日筛选 |
| 7 | 医生端 | 复核详情：确认 / 校正 + 标签选择 + 说明 |
| 8 | 医生端 | 专业版 + 患者简要版双报告 |
| 9 | 用户端 | 注册用户「我的报告」含 AI + 医生双判定 |
| 10 | 用户端 | 快速使用与注册用户均标注待复核，链接可查最终结果 |
| 11 | 数据 | 用户 / 病例 / 判定 / 复核 / 报告持久化 |
| 12 | 数据 | 图像入库并进入复核队列 |
| 13 | 数据 | 校正样本写入新收集的训练样本池 |
| — | 范围 | 首期乳牙滞留，后续扩病种 |

---

*文档结束*
